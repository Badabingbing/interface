{% extends 'base.html.twig' %}

{% block title %}Créer demandeur d'emploi | {{ parent() }}{% endblock %}

{% block body %}
    {{ parent() }}







    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('demandeurs_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
